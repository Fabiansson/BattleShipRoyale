import React from "react";

const statsStyle = {
    backgroundColor: '#525252',
    opacity: '0.8',
    color: 'white',
    height: '100%'
};

function GameStats() {
  return (
    <div style={statsStyle}>
        s
    </div>);
}

export default GameStats;